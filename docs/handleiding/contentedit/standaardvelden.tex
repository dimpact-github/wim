\subsection{Standaardvelden}
Alle content typen bevatten verschillende standaardvelden. De onderstaande lijst beschrijft elk veld apart.

\subsubsection{Titel}
De titel van de pagina, dit veld is altijd verplicht.

\subsubsection{Intro tekst}
De intro tekst van de pagina, dit veld is bij de meeste content typen niet verplicht. Deze tekst zal als introductie teaser worden getoond.

\subsubsection{Body tekst}
Dit veld is het meest belangrijk, in dit veld dient de hoofdtekst ingevuld te worden. Gebruik de editor om de tekst te stijlen met bijvoorbeeld vet gedrukte of cursieve teksten.

\subsubsection{Lead afbeelding}
Klik op ‘Selecteer media’, selecteer een afbeelding en klik op ‘toevoegen’. Selecteer de positie van de afbeelding bij het veld ‘Positie lead afbeelding’. Optioneel kun  je een ondertitel en bron vermelden bij de velden ‘Ondertitel lead afbeelding’ en ‘bron’.

\subsubsection{Afbeeldingen}
Klik op ‘Selecteer media’ bij het veld ‘Afbeeldingen’, selecteer een afbeelding en klik op ‘toevoegen’. Klik vervolgens op ‘Item toevoegen’. Herhaal deze stappen indien je meerdere afbeeldingen wil toevoegen.

\subsubsection{Media}
\pvelist{ \pve{2.16} }
Klik op ‘Select media’ bij het veld ‘Media’, selecteer een bestand en klik op ‘Indienen’. Indien je meerdere mediabestanden wilt toevoegen klik je op ‘Item toevoegen’ en klik vervolgens op ‘Select media’.

Het is ook mogelijk om een video van het web toe te voegen. Klik op ‘Select media’ bij het veld ‘Media’. Klik vervolgens op de tab ‘Web’. Bij het veld ‘URL or embed code’ vul je de URL of embed code in. Dit is een voorbeeld URL om een youtube video toe te voegen:  \texttt{http://www.youtube.com/embed/Abiil-lD3HY} . Om het bestand toe te voegen klik je op ‘Indienen’.

Ook kan er een bestand uit de bibliotheek toegevoegd worden. Klik op ‘Select media’ bij het veld ‘Media’. Klik vervolgens op de tab ‘Bibliotheek’ en selecteer het gewenste bestand. Om het bestand toe te voegen klik je op ‘Indienen’. Toegestane bestandstypen zijn: ‘avi’ (= video) en ‘mp3’ (= audio).

\subsubsection{Attachments}
Klik op ‘Selecteer media’ bij het veld ‘Attachments’, selecteer een bestand en klik op ‘toevoegen’. Klik vervolgens op ‘Item toevoegen’. Herhaal deze stappen indien je meerdere bestanden wil toevoegen.

\subsubsection{Trefwoorden}\label{trefwoorden}
\pvelist{ \pve{2.14}, \pve{2.14.1}, \pve{2.14.2}, \pve{2.14.3} }
Vul in dit veld de belangrijkste kernwoorden in, relevant aan de inhoud van de pagina. De pagina zal gevonden worden wanneer een gebruiker zoekt op de ingevulde trefwoorden.

Bij het invoeren van de trefwoorden zullen automatisch suggesties gegeven worden op basis van trefwoorden die eerder zijn gebruikt (op basis van de eerste letters). Door te klikken op de suggestie of door het gebruik van de pijltjestoetsen kan een suggestie worden overgenomen. Meerdere trefwoorden opgeven kan door de trefwoorden te scheiden met een komma.

\subsubsection{Link}
Vul in dit veld een link in naar een interne of externe pagina. Voorbeeld link: ‘http://www.prorail.nl’. Kruis het vakje ‘URL in nieuw venster openen’ aan om de link in een nieuw venster te laten openen zodra je op de link klikt.

\subsubsection{Subtitel}
Een ‘subtitel’ is een synoniem voor ‘ondertitel’. Deze titel verschijnt onder de hoofdtitel.

\subsubsection{Portret}
Om een portret toe te voegen aan een persoon klik je bij het veld ‘Portret’ op de knop ‘Select media’. Klik vervolgens op de knop ‘Choose file’, selecteer het gewenste bestand en klik daarna op de knop ‘Uploaden’. Klik op de knop ‘Doorgaan’. Vul optioneel een ‘Alt tekst’ en een ‘Titel tekst’ in. Om het proces af te ronden klik je op de knop ‘Opslaan’.

\subsubsection{Tekstopmaak}
\pvelist { \pve{2.18} }
Onder het veld ‘Body’ kan telkens gekozen worden tussen drie verschillende opties: ‘Filtered HTML’, ‘Full HTML’ en ‘Plain text’.
\begin{itemize}
\item Filtered HTML: voorgedefinieerde HTML tags zullen worden toegestaan, alle overige HTML tags zullen uit de tekst gefilterd worden.
\item Full HTML: alle HTML tags zijn toegestaan
\item Plain text:  alleen gewone tekst is toegestaan, alle HTML of andere code zal gefilterd worden zodra de pagina wordt opgeslagen.
\end{itemize}
Binnen \emph{Filtered HTML} is het mogelijk om \emph{iframes} toe te voegen. Dit zijn embedded webpagina's van andere websites. Deze kunnen worden gebruikt om 3th party modules zoals bijv. een enqu\^{e}te in te sluiten.

\subsubsection{Linkit}
\pvelist{ \pve{2.4}, \pve{2.4.1}, \pve{2.4.2}, \pve{2.4.5} }
Wanneer de WYSIWYG editor beschikbaar is, kan er gebruik gemaakt worden van de ‘linkit’ module. Deze module vergemakkelijkt het aanmaken van links naar andere pagina’s. De onderstaande screenshot legt de werking uit.

%TODO: add screenshot

Het volgende venster zal openen, het is mogelijk om een link te verwijzen naar zowel een interne als externe pagina.

\subsubsection{URL alias}\label{alias}
\pvelist{ \pve{2.6}, \pve{2.6.1}, \pve{2.6.2} }
Nieuwe inhoud wordt automatisch op een vriendelijke URL geplaatst (bijv. \\ \texttt{\customerdomain /nieuws/2013/onderwerp}). Dit pad zal normaliter niet aangepast hoeven te worden. De \emph{URL alias} geeft het \emph{gebruikelijke} pad van de pagina weer. Voor een korte versie en andere alternatieve paden dient gebruik gemaakt te worden van een \emph{redirect}\seeone{redirect}.

Het uitgangspunt is dat het automatische pad gehanteerd wordt, maar indien dit onwenselijke resultaten oplevert dan is het voor redacteuren wel mogelijk om dit pad aan te passen. Klik hiervoor bij het toevoegen of bewerken van inhoud op \emph{URL-pad-instellingen} (onderaan de pagina). Daar staat een checkbox die aangeeft dat er een automatische URL gebruikt wordt. Na het uitvinken hiervan kan eronder in een tekstveld een eigen pad worden aangegeven. Vul hiervoor het pad in zonder domeinnaam. Laat ook de eerste en laatste slash weg. Dus \texttt{acties/winteractie} maakt de pagina beschikbaar op \texttt{\customerdomain /acties/winteractie}. Dit pad is relatief t.o.v. de huidige subsite. Dus de alias \texttt{acties/winteractie} op de subsite reizigers heeft als URL \texttt{\customerdomain /reizigers/acties/winteractie}. Deze optie wordt dan ook \emph{niet} gebruikt om inhoud op andere domeinen beschikbaar te maken. Gebruik daarvoor de optie \emph{contentdeling}\seeone{contentdeling}.

Voor de pagina \emph{Waarom wiebelen treinen?} kan bijvoorbeeld de volgende alias en redirect ingesteld worden:
\begin{itemize}
\item \texttt{waarom-wiebelen-treinen} (alias)
\item \texttt{wiebelen} (redirect)
\item \texttt{trillingen} (redirect)
\end{itemize}
Als de pagina de reizigers doelgroep als brondomein heeft\seeone{contentdeling} dan is de primaire URL als volgt:
\begin{verbatim}
prorail.nl/reizigers/waarom-wiebelen-treinen
\end{verbatim}
Tevens is de pagina dan beschikbaar op de onderstaande URL's:
\begin{verbatim}
prorail.nl/reizigers/wiebelen
prorail.nl/wiebelen
prorail.nl/reizigers/trillingen
prorail.nl/trillingen
\end{verbatim}
Wanneer de bezoeker deze URL gebruikt zullen ze automatisch worden doorverwezen naar de primaire URL (de alias).

Het is mogelijk om speciale tekens te gebruiken in de URL alias. Echter raden we het gebruik daarvan sterk af. In veel browsers worden deze getoond met een andere codering (een apostrof wordt bijvoorbeeld "\%E2\%80\%89"). Aanbevolen wordt om de tekens te beperken tot alfanumeriek (a t/m z en 0 t/m 9) en afbreekstreepjes. Het gebruik van hoofdletters is ook niet gebruikelijk in URL's.

Voor project nodes wordt geen alias gebruikt\seeone{projecttoevoegen}.

\subsubsection{Redirects}\label{redirect}
\pvelist{ \pve{2.6.3}, \pve{3.3.7} }
Verkorte URL's en andere alternatieve URL's kunnen ingesteld worden als zogenoemde \emph{redirect} (ook wel: omleiding). Hierbij wordt de bezoeker automatisch doorverwezen naar een andere pagina. Bijv. \texttt{\customerdomain /jaarverslag} kan dan doorverwijzen naar \texttt{\customerdomain /pers/jaarverslag-2013}. Dit kan gebruikt worden om verkorte URL's te maken of om paden die niet meer in gebruik zijn naar een zinvol alternatief te verwijzen.

Ga naar \emph{Instellingen} $\rightarrow$ \emph{URL-omleidingen} om redirects te beheren. Deze pagina is ook direct toegankelijk op \drupalpath{admin/config/search/redirect}. Op deze pagina is een lijst van bestaande redirects te vinden. Klik op \emph{Omleiding toevoegen} om een nieuwe redirect aan te maken.
\begin{enumerate}
\item Vul een pad in dat zal worden omgeleid naar een andere pagina, bijv. \texttt{jaarverslag}. De doelgroep wordt niet meegenomen in het pad.
\item Vul een pad of URL in waar de gebruiker naartoe verwezen moet worden. Dit kan bijvoorbeeld \texttt{jaarverslag-2013} zijn maar ook een volledige URL zoals \texttt{http://www.google.nl/}. Let erop dat bij de interne paden de doelgroep niet wordt meegenomen!
\item Klik op \emph{Opslaan}.
\end{enumerate}

Op het overzicht van omleidingen is ook een tab beschikbaar met de tekst \emph{Verhelp 404 pagina's}. Deze pagina toont een lijst met bezochte paden waarom geen pagina of URL-omleiding aanwezig is. Het is aan te bevelen om redirects te maken van deze paden.

De redirects zullen ook worden gebruikt om paden van de oude website door te verwijzen naar dezelfde pagina op de nieuwe site.

\subsubsection{Uitsluiten voor zoekmachine indexatie}
\pvelist{ \pve{2.3.12} }
Pagina's kunnen worden uitgesloten voor indexatie in zoekmachines (bijv. Google). Ga hiervoor bij het toevoegen of bewerken van inhoud naar de sectie \emph{Meta tags} onderaan de pagina. Hier kan onder andere een paginatitel, beschrijving etc. worden ingevuld. Verder naar onderen staat een link \emph{Uitgebreid}. Na het klikken op die link verschijnen een aantal extra opties, waaronder \emph{Voorkom dat zoekmachines deze pagina indexeren}. Vink deze optie aan om de pagina uit te sluiten voor zoekmachine optimalisatie. Wijzigingen worden niet direct door Google verwerkt. Pagina's die reeds in de zoekmachine zitten worden doorgaans binnen enkele dagen uit de index gehaald.

Merk op dat er ook een optie aanwezig is om aan te geven dat de pagina wel ge\"{i}ndexeerd mag worden. Hiermee wordt dit expliciet aangegeven voor zoekmachines. Wanneer dit niet het geval is zullen zoekmachines de pagina echter ook indexeren. Het is dus niet nodig om deze optie aan te vinken. 


